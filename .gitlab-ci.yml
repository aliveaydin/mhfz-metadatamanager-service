stages:
  - deploy
  - build
  - test

build-and-deploy-server:
  stage: deploy
  image: docker:stable
  services:
    - docker:dind
  script:
    - cp $RSA_PRV_KEY  keys/rsa.key
    - sudo bash -c "docker build -f Dockerfile -t metadataManager-service:latest ."
    - sudo bash -c 'cd undefined/mhfz/  && docker compose up -d metadataManager-service'
  only:
    - main
# Notice: This file is generated by Mindbricks and formatted by prettier-plugin-yaml
